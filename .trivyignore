# Trivy ignore baseline for seqtoid-ci-workflows (the CI SSOT itself).
# CZID-264 model: accept inherited findings here, hard-fail on NEW. Each entry is triaged + justified.
#
# (No active entries. The former flake8-action undici 5.29.0 3x-HIGH baseline
# was retired in #415 by overriding undici to ^6.27.0 in flake8-action/, which
# clears CVE-2026-12151 / CVE-2026-1526 / CVE-2026-2229 at the source.)
